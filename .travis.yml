sudo: required
services:
  - docker
before_install:
  - docker build -t mikhaildedeiko/docker-react -f Dockerfile.dev .
script:
  - docker run -e CI=true mikhaildedeiko/docker-react npm run test -- --coverage
